# PHONE_COLOR_ONLY_PAIR_LABELS_FIX_NOTE

目的: 共通シンボルの視認性を上げるため、同じ `BASE/QUOTE` のライン色だけを共通化する。

## 変更

- `BTC/USDT` など同一ペアは、現物/先物をまたいでも同じライン色。
- 線種による判別は廃止し、すべて実線に統一。
- 右端のエンドポイントラベル、右軸、1280x1920 のスマホ向け構成は維持。

## レイアウト識別子

`phone_portrait_wide_color_only_pair_labels_cum_delta_v9`
